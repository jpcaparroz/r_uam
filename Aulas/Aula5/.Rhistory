#Coeficiente de assimetria
dados = c(18, 20, 20, 21, 22, 24, 25, 25, 26, 27, 29, 29,
30, 30, 31, 31, 32, 33, 34, 35, 36, 36, 37, 37,
37, 37, 38, 38, 38, 40, 41, 43, 44, 44, 45, 45,
45, 46, 47, 48, 49, 50, 51, 53, 54, 54, 56, 58, 62, 65)
CK = kurtosis(dados)
CK = kurtosis(dados)
library(e1071)
CK = kurtosis(dados)
CK
setwd('C:/Users/joao.caparroz/Documents/Eu/Pessoal/Faculdade/Curso em R/Aulas/Aula5')
getwd()
## Estatistica
sleep
View(sleep)
grupodois <- sleep$group == 2
grupoDois <- sleep$group == 2
View(grupoDois)
grupoDois
# Teste Shapiro-wilk
shapiro.test(sleep$extra[grupoDois])
shapiro.test(sleep$extra[!grupoDois])
# Teste F
testeF <- var.test(extra ~ group, data = sleep)
testeF
# Teste T
testeT <- t.test(extra ~ group, data = sleep, var.equal = TRUE)
testeT
## DPLYR
library(dplyr)
## DPLYR
install.packages(dplyr)
library(dplyr)
csv = read.csv('C:/Users/joao.caparroz/Documents/Eu/Pessoal/Faculdade/Curso em R/Aulas/Aula5/files/sleep.csv')
View(csv)
str(csv)
summary(sleep)
summary(csv$sono_total)
csv = read.csv('C:/Users/joao.caparroz/Documents/Eu/Pessoal/Faculdade/Curso em R/Aulas/Aula5/files/sleep.csv')
dadosSono <- select(csv, nome, sono_total)
View(dadosSono)
View(select(csv, nome, where(sonot_total > 2)))
View(select(csv, nome, ?where(sonot_total > 2)))
View(select(csv, nome, ?where(sonot_total > 2)))
?where
?where()
?dplyr
browseVignettes(package = "dplyr")
View(csv %>% select(nome, sono_total))
View(select(csv, nome:pais))
View( select(csv, filter(csv, sono_total > 10)))
View( filter(csv, sono_total > 10))
View( filter(csv, nome == 'Carlos'))
# Filter
View( filter(csv, sono_total < 10))
View( filter(csv, nome == 'Carlos', sono_total))
View( filter(select(csv, nome, sono_total), nome == 'Carlos'))
View( filter(select(csv, nome, sono_total), nome == 'Pedro'))
View( filter(select(csv, nome, sono_total), nome == 'Rafael'))
View( filter(select(csv, nome, pais, sono_total), nome == 'Rafael'))
View( filter(select(csv, nome, pais, sono_total), sono_total > 10 ))
View( filter(select(csv, nome, pais, sono_total), sono_total > 10 ))
matrix(filter(select(csv, nome, pais, sono_total), sono_total > 10 ))
matrix(filter(select(csv, nome, pais, sono_total), sono_total > 10 ),
nrow = 3)
matrix(filter(select(csv, nome, pais, sono_total), sono_total > 10 ),
nrow = 3, byrow = T)
pie( filter(select(csv, nome, pais, sono_total), sono_total > 10 ),
col = class)
pie( filter(select(csv, nome, sono_total), sono_total > 10 ),
col = class)
pie( filter(select(csv, nome, sono_total), sono_total > 10 ),
labels = nome
col = class)
pie( filter(select(csv, nome, sono_total), sono_total > 10 ),
labels = nome,
col = class)
pie( filter(select(csv, pais, sono_total), sono_total > 10 ),
labels = nome,
col = class)
pie( filter(select(csv, pais, sono_total), sono_total > 10 ),
col = class,
main = graficos)
pie( filter(select(csv, pais, sono_total), sono_total > 10 ),
col = class,
main = 'graficos')
pie( filter(select(csv, pais, sono_total), sono_total > 10 ),
col = c('red', 'blue'),
main = 'graficos')
pie( filter(select(csv, sono_total, pais), sono_total > 10 ),
col = class,
main = 'graficos')
View( arrange(cidade))
View( arrange(select(csv, arrange(cidade))))
View( arrange(select(csv, arrange(cidade)))
View(
View(
csv
)
View(
arrange(
select(
csv, cidade
)
)
)
View(
arrange(
select(
csv, cidade
)
)
)
View(
arrange(
select(
csv, desc(cidade)
)
)
)
View(
arrange(
select(
csv, desc(cidade)
)
)
)
View(
csv %>% select(nome, cidade, sono_total) %>%
arrange(cidade, sono_total) %>%
head(5)
)
barplot( filter(select(csv, sono_total, pais), sono_total > 10 ),
col = class,
main = 'graficos')
barplot( filter(select(csv, sono_total, pais), sono_total > 10 ),
beside = T,
col = class,
main = 'graficos')
filter(select(csv, sono_total, pais), sono_total > 10
View(filter(select(csv, sono_total, pais), sono_total > 10)
View(filter(select(csv, sono_total, pais), sono_total > 10)
View(filter(select(csv, sono_total, pais), sono_total > 10)
View(filter(select(csv, sono_total, pais), sono_total > 10))
barplot( filter(select(csv, pais, sono_total), sono_total > 10 ),
beside = T,
col = class,
main = 'graficos')
barplot( c(filter(select(csv, pais, sono_total), sono_total > 10 )),
beside = T,
col = class,
main = 'graficos')
barplot( c(filter(select(csv, pais, sono_total), sono_total > 10 )),
beside = T,
col = class,
main = 'graficos')
barplot( filter(csv,sono_total > 10 ),
beside = T,
col = class,
main = 'graficos')
barplot( c(filter(csv,sono_total > 10 )),
beside = T,
col = class,
main = 'graficos')
# Filter
View( filter(csv, sono_total < 10))
barplot( c(filter(select(csv, sono_total) ,sono_total > 10 )),
beside = T,
col = class,
main = 'graficos')
str(csv)
?table
View(c(filter(select(csv, sono_total) ,sono_total > 10 )))
barplot( c(filter(select(csv, sono_total) ,sono_total > 10 )),
beside = T,
col = class,
main = 'graficos')
View( filter(select(csv, nome, pais, sono_total), sono_total > 10 ))
barplot( filter(select(csv, pais, sono_total), sono_total > 10 ),
beside = T,
col = class,
main = 'graficos')
View( filter(select(csv, pais, sono_total), sono_total > 10 ))
plot( filter(select(csv, pais, sono_total), sono_total > 10 ),
col = class,
main = 'graficos')
plot( filter(select(csv, peso, sono_total), sono_total > 10 ),
col = class,
main = 'graficos')
plot( filter(select(csv, peso, sono_total), sono_total < 16 ),
col = class,
main = 'graficos')
plot( filter(select(csv, peso, sono_total), sono_total < 16 ),
col = c('red', 'blue'),
main = 'graficos')
View(
csv %>%
select(nome, cidade, sono_total) %>%
arrange(cidade, desc(sono_total)) %>%
filter(sono_total >= 10)
)
View(
mutate(
select(csv,nome, sono_total, peso),
novo = sono_total / peso
)
)
View(
summarise(
select(csv, sono_total),
media_sono = mean(sono_total)
)
)
View(
summarise(
select(csv, sono_total),
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var.test(sono_total)
)
)
View(
summarise(
select(csv, sono_total),
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var.test(sono_total)
)
)
View(
summarise(
select(csv, sono_total),
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = test(sono_total)
)
)
View(
summarise(
select(csv, sono_total),
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var(sono_total)
)
)
View(
summarise(
select(csv, sono_total),
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var(sono_total),
total = n()
)
)
View(
group_by(cidade)
summarise(
View(
csv %>%
summarise(
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var(sono_total),
total = n() #total de elementos
)
)
View(
csv %>%
group_by(cidade)
summarise(
View(
csv %>%
group_by(cidade) %>%
summarise(
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var(sono_total),
total = n() #total de elementos
)
)
View(
csv %>%
group_by(cidade) %>%
summarise(
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var(sono_total),
total = n()
)
)
View(
csv %>%
group_by(pais) %>%
summarise(
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var(sono_total),
total = n()
)
)
View(csv)
View(
csv %>%
group_by(peso) %>%
summarise(
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var(sono_total),
total = n()
)
)
View(
csv %>%
group_by(pais) %>%
summarise(
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var(sono_total),
total = n()
)
)
View(
csv %>%
group_by(pais,cidade) %>%
summarise(
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var(sono_total),
total = n()
)
)
View(
csv %>%
select(cidade, pais, sono_total) %>%
summarise(
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var(sono_total),
total = n()
)
)
View(
csv %>%
group_by(pais,cidade) %>%
filter(sono_total > 10) %>%
summarise(
media = mean(sono_total),
maior = max(sono_total),
menor = min(sono_total),
var = var(sono_total),
total = n()
)
)
